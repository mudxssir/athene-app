# Phase 0 Report — Stop the Bleeding

_Completed 2026-06-11 · Branch `pipeline/p0-stop-the-bleeding` (9 commits) · All work
tracked in `P0_TRACKER.md`._

## Outcome

8/8 tickets done (P0-5b, an XS admin-UI row, split out and carried into P1 scope).

| Ticket | Commit | Delivered |
|---|---|---|
| P0-1 | `bfb9d1a` | Decision extraction fires for `google`/`microsoft`/`direct_upload` (audit D1 interim) |
| P0-2 | `bfb9d1a`,`c9bc742` | `query` task hint end to end (vector-search ×2, entity-resolver, Jina task mapping) |
| P0-3 | `58b26e6`,`18f1f15` | `embedding_model`/`pipeline_version`/`shape` columns; rows stamped via `embedBatchDetailed()` |
| P0-4 | `58b26e6` | Per-hint embedding batch groups, alignment test-verified |
| P0-5 | `84b0db2` | Skip-sentinel drop + `sync_skips` telemetry + admin endpoint + Drive branch |
| P0-6 | `9ba3b18` | `chunk-text-store.ts` choke point + CI grep gate (encryption flip = 2 functions) |
| P0-7 | `58b26e6`,`5fc9fda` | FALLBACK ACTIVATED + normalize-strip telemetry, with synthetic test |
| P0-8 | `a8491b4` | Eval harness, 5-shape golden sets, baseline recorded |

## Baseline (gate reference for P1+)

`baselines/p0.json` · model **jina-embeddings-v3** · pipeline_version 1 · top-5, doc-level:

| shape | recall@5 | MRR |
|---|---|---|
| prose | 1.0000 | 0.9545 |
| email | 0.9818 | 0.7597 |
| work_item | 0.9818 | 0.7439 |
| thread | 0.9636 | 0.7648 |
| tabular | 1.0000 | 0.7724 |
| **macro** | **0.9854** | **0.7991** |

Recall is near-ceiling on synthetic fixtures (relevance correct by construction); **MRR is
the sensitive gate metric** for P1+ (0.74–0.80 leaves headroom). Determinism: two
consecutive runs — recall Δ 0.0000, MRR Δ 0.0018 (<2% AC) PASS.

> **⚠ Baseline stale after P0-8 post-review fixes.** Fixtures were regenerated (F-B dedup
> fix + F-A `prose_long` shape). Re-record before using these numbers as the P1 gate:
> ```
> npx tsx scripts/eval/run-eval.ts --out docs/plans/phase-reports/baselines/p0.json
> ```

## Gate checklist

- [x] All tickets done, ACs checked (P0-5b carried to P1).
- [x] Baselines recorded (`baselines/p0.json`).
- [x] Decision nodes from a Drive/Gmail-source fixture (extractor dual-prompt test).
- [~] Mixed-model SQL assertion: **mechanism shipped** (column + stamping + search-side
  enforcement deferred to P1 pinning); live SQL check runs after first deploy applies the
  migration — code-level tests green.
- [~] Rollback drill: production-code hunks of `bfb9d1a` revert cleanly; the test file
  conflicts (P0-7 appended tests to the same region) needing one manual resolution.
  Acceptable: rollback path is feature-level (the three string-set entries + hint args are
  independently deletable); recorded as drill finding.

## Deviations & findings

1. Migration version collision (`20260611000001` taken) — renamed, fixup `18f1f15`.
   **Protocol addition:** check existing max migration version before authoring.
2. Latent test-infra bug: non-constructible `openai` mock (arrow-fn under `new`) — fixed.
3. Pre-existing failures confirmed on `main` (not P0): action-executor 13, sharepoint 3,
   onedrive 1, looker 2, tableau 2, redshift 1, rbac 1, calendar-agent 1 — needs its own
   cleanup ticket outside the pipeline program.
4. `providers.test.ts` count tripwire was stale (27th provider `github-getting-started`).
5. Eval note: email fixtures currently chunk via the *document* family (audit D3/D4
   mismatch is still live until P1 shape routing) — the baseline deliberately measures
   current behavior, so P1's routing fix will show up as an email/thread MRR delta.

## Next: Phase 1 entry criteria

P1 (shape routing, sidecar skeleton, Jina pinning + re-embed, dynamic chunk policy) can
start immediately; its gate compares against this baseline (≥15% recall@5 — given recall
ceiling, interpret as macro MRR +15% relative OR per-shape recall non-regression with MRR
improvement; re-state in P1 tracker).
